# 🎴 MangaBuff Auto Trading Bot

Автоматизированный бот для обмена карточками и участия в клубных активностях на сайте MangaBuff.

## ⚠️ Важные изменения в последней версии

### 🔄 Версия 2.0 - Статистика с сервера

**Критическое изменение:** Статистика теперь парсится **напрямую со страницы клуба**, а не хранится локально.

**Что это означает:**
- ✅ Всегда актуальные данные о пожертвованиях и заменах
- ✅ Синхронизация с реальным состоянием на сервере
- ✅ Нет риска рассинхронизации
- ⚠️ Требуется стабильное интернет-соединение

---

## 📋 Возможности

### Основной функционал:
- 🤖 **Автоматическая обработка владельцев карт** с отправкой обменов
- 📊 **Парсинг статистики** напрямую со страницы клуба
- 🔄 **Мониторинг буста** с автоматическим внесением карт
- 🔀 **Автозамена карт** когда владельцев меньше 50
- 💾 **Управление инвентарем** с кэшированием
- 🎯 **Умный подбор карт** для обмена

### Дополнительные функции:
- Пропуск первых 6 владельцев на первой странице
- Задержки между обменами (11+ секунд)
- Dry-run режим для тестирования
- Детальный debug режим
- Автоотмена обменов при смене карты

---

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

**requirements.txt:**
```
requests>=2.31.0
beautifulsoup4>=4.12.0
```

### 2. Базовый запуск

```bash
python main.py \
    --email ваш_email@example.com \
    --password ваш_пароль \
    --user_id ваш_id \
    --boost_url https://mangabuff.ru/clubs/klub-taro-2/boost \
    --enable_monitor
```

### 3. Проверка работы (dry-run)

```bash
python main.py \
    --email ваш_email@example.com \
    --password ваш_пароль \
    --user_id ваш_id \
    --boost_url https://mangabuff.ru/clubs/klub-taro-2/boost \
    --dry_run
```

---

## 📖 Подробное использование

### Параметры командной строки

#### Обязательные:
- `--email` - Email для входа на сайт
- `--password` - Пароль
- `--user_id` - Ваш ID пользователя на MangaBuff

#### Опциональные:
- `--boost_url` - URL страницы буста клуба (обязателен для работы со статистикой)
- `--enable_monitor` - Включить мониторинг страницы буста
- `--skip_inventory` - Пропустить загрузку инвентаря
- `--only_list_owners` - Только вывести список владельцев без обменов
- `--dry_run` - Тестовый режим (без реальных обменов)
- `--debug` - Режим отладки с подробным выводом

### Режимы работы

#### 1. Полная автоматизация (рекомендуется)

```bash
python main.py \
    --email user@mail.com \
    --password pass123 \
    --user_id 12345 \
    --boost_url https://mangabuff.ru/clubs/klub-taro-2/boost \
    --enable_monitor
```

**Что делает:**
- Загружает инвентарь
- Получает информацию о карте для буста
- Обрабатывает всех доступных владельцев
- Мониторит страницу клуба каждые 2 секунды
- Автоматически вносит карту при появлении буста
- Автоматически заменяет карту если владельцев ≤ 50
- Перезапускает обработку при смене карты

#### 2. Однократная обработка (без мониторинга)

```bash
python main.py \
    --email user@mail.com \
    --password pass123 \
    --user_id 12345 \
    --boost_url https://mangabuff.ru/clubs/klub-taro-2/boost
```

**Что делает:**
- Загружает инвентарь
- Обрабатывает владельцев один раз
- Завершает работу

#### 3. Только список владельцев

```bash
python main.py \
    --email user@mail.com \
    --password pass123 \
    --user_id 12345 \
    --boost_url https://mangabuff.ru/clubs/klub-taro-2/boost \
    --only_list_owners
```

**Что делает:**
- Выводит список всех доступных владельцев онлайн без замка
- Не отправляет обмены

#### 4. Тестирование (dry-run)

```bash
python main.py \
    --email user@mail.com \
    --password pass123 \
    --user_id 12345 \
    --boost_url https://mangabuff.ru/clubs/klub-taro-2/boost \
    --dry_run \
    --debug
```

**Что делает:**
- Эмулирует все действия без реальных запросов
- Выводит подробную отладочную информацию
- Безопасно для тестирования логики

---

## 📊 Система статистики

### Откуда берутся данные

Бот парсит статистику **напрямую со страницы клуба**:

#### Замены карт:
```html
<div class="club-boost__change">
    <div><span>2</span> / 10</div>  <!-- Использовано 2 из 10 -->
</div>
```

#### Пожертвования:
```html
<ul class="club-boost__rules">
    <li>В день можно пожертвовать до 5/50 карт...</li>  <!-- 5 из 50 -->
</ul>
```

### Пример вывода:

```
📊 Дневная статистика (с сервера):
   Пожертвовано: 5/50
   Замен карты: 2/10
   Осталось пожертвований: 45
   Осталось замен: 8
```

### Обновление статистики:

- **При старте бота** - загружается с сервера
- **Перед каждым действием** - проверка лимитов с сервера
- **После каждого действия** - обновление с сервера
- **При проверке мониторинга** - периодическое обновление

---

## 🔧 Настройка параметров

Все настройки находятся в файле `config.py`:

### Основные параметры:

```python
# Задержки между обменами
MIN_TRADE_DELAY = 11.0  # Минимум 11 секунд между обменами
TRADE_RANDOM_DELAY_MIN = 0.5  # Минимальная случайная задержка
TRADE_RANDOM_DELAY_MAX = 2.0  # Максимальная случайная задержка

# Мониторинг
MONITOR_CHECK_INTERVAL = 2  # Проверка каждые 2 секунды
MONITOR_STATUS_INTERVAL = 30  # Вывод статуса каждые 30 проверок

# Автозамена карт
MAX_CLUB_CARD_OWNERS = 50  # Порог владельцев для автозамены

# Пропуск владельцев
FIRST_PAGE_SKIP_OWNERS = 6  # Пропустить первых N на первой странице
```

### Таймауты:

```python
REQUEST_TIMEOUT = (4, 8)  # (connect timeout, read timeout)
```

---

## 📁 Структура файлов

```
mangabuff_bot/
├── main.py                    # Главный файл запуска
├── auth.py                    # Авторизация
├── boost.py                   # Работа с буст-картами
├── card_replacement.py        # Автозамена карт
├── card_selector.py           # Подбор карт для обмена
├── config.py                  # Конфигурация
├── daily_stats.py             # Парсинг статистики с сайта ⭐
├── inventory.py               # Управление инвентарем
├── monitor.py                 # Мониторинг буста
├── owners_parser.py           # Парсинг владельцев
├── parsers.py                 # Вспомогательные парсеры
├── trade.py                   # Обмены карточками
├── utils.py                   # Утилиты
├── requirements.txt           # Зависимости
└── created_files/             # Генерируемые файлы
    ├── inventory.json         # Ваш инвентарь
    ├── parsed_inventory.json  # Пропарсенный инвентарь
    └── boost_card.json        # Текущая карта для буста
```

---

## 🎯 Алгоритм работы

### 1. Инициализация
```
Вход в аккаунт → Загрузка инвентаря → Парсинг статистики
```

### 2. Получение карты для буста
```
Загрузка страницы → Извлечение ID → Получение деталей → Проверка на автозамену
```

### 3. Обработка владельцев (постранично)
```
Для каждой страницы:
  └─ Для каждого владельца онлайн без замка:
       ├─ Подбор подходящей карты из инвентаря
       ├─ Проверка на изменение карты в клубе
       ├─ Отправка обмена (если карта не изменилась)
       └─ Задержка 11+ секунд
```

### 4. Мониторинг (если включен)
```
Каждые 2 секунды:
  ├─ Проверка смены карты в клубе
  ├─ Проверка доступности буста
  └─ При обнаружении:
       ├─ Проверка лимитов (парсинг с сайта)
       ├─ Внесение карты
       ├─ Отмена всех обменов
       └─ Перезапуск обработки с новой картой
```

---

## 💡 Примеры использования

### Запуск на весь день с автоматизацией

```bash
# Linux/Mac
nohup python main.py \
    --email user@mail.com \
    --password pass123 \
    --user_id 12345 \
    --boost_url https://mangabuff.ru/clubs/klub-taro-2/boost \
    --enable_monitor > bot.log 2>&1 &

# Windows (PowerShell)
Start-Process python -ArgumentList "main.py --email user@mail.com --password pass123 --user_id 12345 --boost_url https://mangabuff.ru/clubs/klub-taro-2/boost --enable_monitor" -WindowStyle Hidden
```

### Проверка конкретной карты

```bash
python main.py \
    --email user@mail.com \
    --password pass123 \
    --user_id 12345 \
    --boost_url https://mangabuff.ru/clubs/klub-taro-2/boost \
    --only_list_owners
```

### Отладка проблем

```bash
python main.py \
    --email user@mail.com \
    --password pass123 \
    --user_id 12345 \
    --boost_url https://mangabuff.ru/clubs/klub-taro-2/boost \
    --dry_run \
    --debug > debug.log 2>&1
```

---

## 🐛 Troubleshooting

### Проблема: "Ошибка загрузки страницы буста"

**Решение:**
1. Проверьте правильность URL
2. Убедитесь что авторизация прошла успешно
3. Проверьте интернет-соединение

### Проблема: "Достигнут дневной лимит"

**Решение:**
- Это нормально - лимиты устанавливает сайт
- Статистика парсится с сервера, данные точные
- Подождите до следующего дня (сброс в 00:00 МСК)

### Проблема: "Не удалось подобрать карту"

**Решение:**
1. Проверьте что в инвентаре есть карты нужного ранга
2. Убедитесь что файл `inventory.json` не пустой
3. Попробуйте перезагрузить инвентарь (без `--skip_inventory`)

### Проблема: "Карта не найдена у владельца"

**Возможные причины:**
- Владелец уже обменял карту
- Владелец закрыл обмены
- Временная проблема с сетью
- Бот автоматически продолжит со следующим владельцем

---

## ⚙️ Продвинутая настройка

### Изменение алгоритма подбора карт

Редактируйте `card_selector.py`:

```python
# Приоритет карт с минимальным количеством желающих
if card_data["wanters_count"] < target_wanters:
    return parsed_card
```

### Настройка таймаутов

Редактируйте `config.py`:

```python
REQUEST_TIMEOUT = (5, 10)  # Увеличить для медленного соединения
MIN_TRADE_DELAY = 15.0     # Увеличить задержку между обменами
```

### Изменение количества пропускаемых владельцев

```python
FIRST_PAGE_SKIP_OWNERS = 10  # Пропускать первых 10 вместо 6
```

---

## 📈 Рекомендации по использованию

### Для максимальной эффективности:

1. **Используйте мониторинг** (`--enable_monitor`)
   - Автоматическое внесение при бусте
   - Автоматическая замена карт
   - Непрерывная работа

2. **Регулярно обновляйте инвентарь**
   - Запускайте без `--skip_inventory`
   - Особенно после крупных обменов

3. **Следите за логами**
   - Перенаправляйте вывод в файл
   - Проверяйте наличие ошибок

4. **Используйте dry-run для тестирования**
   - Перед изменением настроек
   - При обновлении кода

### Оптимальная стратегия:

```
Утро:   Запуск с обновлением инвентаря + мониторинг
День:   Бот работает автоматически
Вечер:  Проверка логов, анализ результатов
```

---

## 📜 История версий

### v2.0 (Текущая) - Статистика с сервера
- ✅ Полностью переработан модуль статистики
- ✅ Парсинг данных напрямую со страницы клуба
- ✅ Улучшена синхронизация с сервером
- ✅ Добавлено кэширование запросов
- ✅ Улучшена обработка ошибок

### v1.0 - Первая версия
- Базовый функционал обменов
- Мониторинг буста
- Автозамена карт
- Управление инвентарем

---

## 🤝 Поддержка

При возникновении проблем:

1. Проверьте `debug.log` с параметром `--debug`
2. Убедитесь в актуальности зависимостей
3. Проверьте доступность сайта MangaBuff
4. Создайте issue с подробным описанием проблемы

---

## ⚖️ Disclaimer

Данный бот создан исключительно в образовательных целях. Автор не несет ответственности за любые последствия использования бота. Используйте на свой страх и риск и в соответствии с правилами сайта MangaBuff.

---

## 📄 Лицензия

MIT License - свободное использование с указанием автора.

---

**Версия:** 2.0  
**Дата обновления:** 13 января 2026  
**Статус:** Stable ✅