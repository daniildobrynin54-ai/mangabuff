# MangaBuff - Автоматизация обменов карт

Автоматический бот для обмена карточками на MangaBuff.ru с поддержкой мониторинга клубных бустов.

## 🚀 Возможности

- **Автоматическая загрузка инвентаря** - получение всех карт пользователя
- **Умный подбор карт** - выбор оптимальных карт для обмена на основе количества желающих
- **Постраничная обработка владельцев** - поиск доступных владельцев онлайн
- **Автоматическая отправка обменов** - с учетом задержек и rate limiting
- **Мониторинг клубных бустов** - автоматическое внесение карт когда они становятся доступны
- **Кэширование** - сохранение данных о картах для ускорения работы
- **Dry-run режим** - тестирование без реальных обменов

## 📦 Установка

### Требования

- Python 3.7+
- pip

### Установка зависимостей

```bash
pip install -r requirements.txt
```

## 🎮 Использование

### Базовое использование

```bash
python main.py \
  --email your@email.com \
  --password your_password \
  --user_id 12345 \
  --boost_url https://mangabuff.ru/clubs/123/boost
```

### Тестовый режим (без реальных обменов)

```bash
python main.py \
  --email your@email.com \
  --password your_password \
  --user_id 12345 \
  --boost_url https://mangabuff.ru/clubs/123/boost \
  --dry_run
```

### Только вывод списка владельцев

```bash
python main.py \
  --email your@email.com \
  --password your_password \
  --user_id 12345 \
  --boost_url https://mangabuff.ru/clubs/123/boost \
  --only_list_owners
```

### С мониторингом буста

```bash
python main.py \
  --email your@email.com \
  --password your_password \
  --user_id 12345 \
  --boost_url https://mangabuff.ru/clubs/123/boost \
  --enable_monitor
```

### Пропустить загрузку инвентаря

```bash
python main.py \
  --email your@email.com \
  --password your_password \
  --user_id 12345 \
  --boost_url https://mangabuff.ru/clubs/123/boost \
  --skip_inventory
```

## 📖 Параметры командной строки

| Параметр | Обязательный | Описание |
|----------|--------------|----------|
| `--email` | Да | Email для входа |
| `--password` | Да | Пароль |
| `--user_id` | Да | ID пользователя |
| `--boost_url` | Нет | URL страницы буста клуба |
| `--skip_inventory` | Нет | Пропустить загрузку инвентаря |
| `--only_list_owners` | Нет | Только вывести список владельцев |
| `--enable_monitor` | Нет | Включить мониторинг буста |
| `--dry_run` | Нет | Тестовый режим без реальных обменов |
| `--debug` | Нет | Режим отладки |

## 🏗️ Структура проекта

```
mangabuff/
├── config.py              # Конфигурация
├── utils.py               # Вспомогательные функции
├── auth.py                # Авторизация
├── parsers.py             # Парсеры владельцев и желающих
├── inventory.py           # Управление инвентарем
├── boost.py               # Работа с клубными бустами
├── card_selector.py       # Селектор карт для обмена
├── trade.py               # Обмены карточками
├── owners_parser.py       # Парсинг владельцев карт
├── monitor.py             # Мониторинг буста
├── main.py                # Главный модуль
├── requirements.txt       # Зависимости
└── created_files/         # Папка с данными (создается автоматически)
    ├── inventory.json           # Инвентарь пользователя
    ├── parsed_inventory.json    # Кэш пропарсенных карт
    └── boost_card.json          # Информация о буст-карте
```

## ⚙️ Алгоритм работы

### 1. Подбор карт для обмена

Алгоритм подбирает карты того же ранга с меньшим количеством желающих:

1. Выбирает случайную карту из инвентаря нужного ранга
2. Парсит количество желающих
3. Если желающих меньше чем у целевой карты - подходит
4. Если больше - пробует другую карту
5. При исчерпании непропарсенного инвентаря использует кэш
6. Выбирает оптимальную карту из доступных

### 2. Обработка владельцев

1. Парсит страницу владельцев постранично
2. Фильтрует владельцев (только онлайн, без замка)
3. Для каждого подбирает карту
4. Отправляет обмены с задержками (минимум 11 секунд)
5. Переходит к следующей странице
6. **При окончании владельцев** - ожидает 5 минут или буст
7. **Если буст** - переход на новую карту
8. **Если таймаут** - отмена обменов и перезапуск с той же картой

### 3. Мониторинг буста

1. Проверяет доступность кнопки пожертвования каждые 2 секунды
2. При обнаружении доступности - вносит карту
3. Отменяет все отправленные обмены
4. Загружает информацию о новой карте
5. При изменении карты - перезапускает обработку владельцев

### 4. Цикл ожидания после обработки всех владельцев

**Сценарий:** Все доступные владельцы обработаны, но буст не произошел

**Действия:**
1. ⏳ Ожидание 5 минут
2. 🔍 Продолжение мониторинга буста каждые 2 секунды
3. **Если буст произошел** → Переход на новую карту
4. **Если таймаут (5 минут)** → Отмена всех обменов + Перезапуск с той же картой

**Зачем это нужно:**
- За 5 минут владельцы могут изменить статус (онлайн/офлайн)
- Могут появиться новые владельцы
- Можно повторно отправить обмены тем же людям

**Пример лога:**
```
✅ Обработка завершена:
   Проверено владельцев: 6
   Отправлено обменов: 6

============================================================
⏳ ВСЕ ВЛАДЕЛЬЦЫ ОБРАБОТАНЫ - Ожидание буста 5 минут
============================================================
   Текущая карта: ID 323752
   Мониторинг продолжает работать...
   Если буст произойдет - автоматический переход на новую карту
   Если буст НЕ произойдет - перезапуск обработки с той же картой

⏳ Ожидание: прошло 30с, осталось 270с
⏳ Ожидание: прошло 60с, осталось 240с
...
⏱️  ТАЙМАУТ: прошло 5 минут без буста
   Отменяем обмены и перезапускаем обработку с той же картой...

🔄 Отменяем все отправленные обмены...
✅ Все обмены отменены!

============================================================
🔄 ПЕРЕЗАПУСК: Начинаем обработку ТОЙ ЖЕ карты заново
============================================================

🎯 Обработка карты: Е Нянь (ID: 323752)
...
```

## 🔒 Безопасность

- **Rate limiting**: Соблюдаются задержки между запросами
- **Dry-run режим**: Тестирование без реальных действий
- **Валидация данных**: Проверка всех входных данных
- **Обработка ошибок**: Graceful degradation при ошибках сети

## 📊 Кэширование

Программа кэширует информацию о картах на 24 часа для ускорения работы:

- Количество желающих
- Метаданные карт
- Instance ID карт

Кэш автоматически инвалидируется по истечении времени.

## 🐛 Отладка

Для включения режима отладки используйте флаг `--debug`:

```bash
python main.py --email user@example.com --password pass --user_id 12345 --debug
```

В режиме отладки выводится:
- Детальная информация о запросах
- Статусы ответов
- Отладочные сообщения

## ⚠️ Важные замечания

1. **Задержки**: Между обменами соблюдается минимальная задержка 11 секунд для соблюдения лимитов API
2. **Первая страница**: На первой странице владельцев пропускаются первые 6 записей
3. **Приближенный подсчет**: Для карт с большим количеством страниц используется оптимизированный подсчет
4. **Мониторинг**: При включенном мониторинге программа работает до прерывания (Ctrl+C)
5. **Цикл ожидания**: После обработки всех владельцев программа ждет 5 минут:
   - Если буст произошел → переход на новую карту
   - Если таймаут → отмена обменов и перезапуск с той же картой
6. **Повторные обмены**: Программа может отправлять обмены одним и тем же владельцам после таймаута (это нормально)

## 📝 Логирование

Программа выводит цветные статусы:
- ✅ - Успешные операции
- ❌ - Ошибки
- ⚠️ - Предупреждения
- 🔍 - Поиск/парсинг
- 📊 - Статистика
- 🎯 - Важные события

## 🤝 Вклад в проект

Проект открыт для улучшений! Основные направления:

- Улучшение алгоритмов подбора карт
- Оптимизация производительности
- Добавление новых функций
- Улучшение обработки ошибок

## 📜 Лицензия

Этот проект предназначен для образовательных целей.

## ⚡ Производительность

- **Пагинация**: Оптимизированная загрузка данных постранично
- **Кэширование**: Минимизация повторных запросов
- **Параллелизм**: Мониторинг в отдельном потоке
- **Приближенные оценки**: Быстрый подсчет для больших списков