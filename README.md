# MangaBuff Bot - Рефакторинг v2.0

## 🆕 Что нового

### ✅ Исправлены критические ошибки

1. **Ошибка 422 "Карта уже в обмене"** - исправлен поиск карт у владельцев с большим количеством карт
   - Проблема: offset работал неправильно для владельцев с >9999 картами
   - Решение: правильная работа с батчами по 10000 карт (0-9999, 10000-19999, и т.д.)

2. **Ошибка 429 "Rate Limit"** - добавлена полная система rate limiting
   - Лимит: 66 запросов в минуту
   - Автоматическая пауза 15 секунд при получении 429
   - Умное распределение запросов во времени

3. **Поддержка прокси** - теперь можно работать через прокси
   - HTTP/HTTPS прокси
   - SOCKS5 прокси
   - Авторизация на прокси

### 🔧 Улучшения архитектуры

- Рефакторинг кода для лучшей читаемости
- Централизованный rate limiter
- Retry логика с exponential backoff
- Улучшенная обработка ошибок
- Более информативные логи

## 📦 Установка

```bash
# Клонируйте репозиторий
git clone <repository-url>
cd mangabuff-bot

# Установите зависимости
pip install -r requirements.txt
```

## 🚀 Использование

### Базовый запуск

```bash
python main.py \
  --email your@email.com \
  --password yourpassword \
  --user_id 12345 \
  --boost_url "https://mangabuff.ru/clubs/123/boost"
```

### Запуск с прокси

#### Через аргументы командной строки:

```bash
# HTTP прокси
python main.py \
  --email your@email.com \
  --password yourpassword \
  --user_id 12345 \
  --boost_url "https://mangabuff.ru/clubs/123/boost" \
  --proxy "http://proxy.example.com:8080"

# HTTP прокси с авторизацией
python main.py \
  --email your@email.com \
  --password yourpassword \
  --user_id 12345 \
  --boost_url "https://mangabuff.ru/clubs/123/boost" \
  --proxy "http://username:password@proxy.example.com:8080"

# SOCKS5 прокси
python main.py \
  --email your@email.com \
  --password yourpassword \
  --user_id 12345 \
  --boost_url "https://mangabuff.ru/clubs/123/boost" \
  --proxy "socks5://proxy.example.com:1080"
```

#### Через файл с прокси:

Создайте файл `proxy.txt` с одной строкой:
```
http://username:password@proxy.example.com:8080
```

Затем запустите:
```bash
python main.py \
  --email your@email.com \
  --password yourpassword \
  --user_id 12345 \
  --boost_url "https://mangabuff.ru/clubs/123/boost" \
  --proxy_file proxy.txt
```

#### Через переменную окружения:

```bash
# Linux/Mac
export PROXY_URL="http://proxy.example.com:8080"
python main.py --email ... --password ... --user_id ... --boost_url ...

# Windows CMD
set PROXY_URL=http://proxy.example.com:8080
python main.py --email ... --password ... --user_id ... --boost_url ...

# Windows PowerShell
$env:PROXY_URL="http://proxy.example.com:8080"
python main.py --email ... --password ... --user_id ... --boost_url ...
```

### Дополнительные опции

```bash
# Включить мониторинг буста
--enable_monitor

# Пропустить загрузку инвентаря
--skip_inventory

# Только показать владельцев (без обменов)
--only_list_owners

# Тестовый режим (без реальных обменов)
--dry_run

# Режим отладки (подробные логи)
--debug
```

### Пример полной команды

```bash
python main.py \
  --email myemail@example.com \
  --password mypassword \
  --user_id 12345 \
  --boost_url "https://mangabuff.ru/clubs/123/boost" \
  --proxy "http://user:pass@proxy.com:8080" \
  --enable_monitor \
  --debug
```

## ⚙️ Конфигурация

Основные настройки находятся в `config.py`:

### Rate Limiting
```python
RATE_LIMIT_PER_MINUTE = 66      # Максимум запросов в минуту
RATE_LIMIT_RETRY_DELAY = 15     # Пауза при 429 (секунды)
RATE_LIMIT_WINDOW = 60          # Окно для подсчета запросов
```

### Прокси
```python
PROXY_ENABLED = True            # Включить/выключить прокси
PROXY_URL = None                # URL прокси (по умолчанию из аргументов)
```

### Поиск карт
```python
CARDS_PER_BATCH = 10000         # Размер батча (0-9999, 10000-19999, ...)
```

### Retry логика
```python
MAX_RETRIES = 3                 # Максимум попыток при ошибках
RETRY_DELAY = 2                 # Базовая задержка между попытками
```

## 🔍 Как работает Rate Limiting

1. **Подсчет запросов**: Скрипт отслеживает все запросы в скользящем окне 60 секунд
2. **Автоматическое ожидание**: При приближении к лимиту скрипт автоматически замедляется
3. **Обработка 429**: При получении 429 скрипт ставит все запросы на паузу 15 секунд
4. **Умное распределение**: Запросы распределяются равномерно во времени

### Запросы, которые считаются в лимите:

- ✅ Отправка обмена
- ✅ Загрузка страницы владельцев (`/cards/{id}/users`)
- ✅ Загрузка страницы желающих (`/cards/{id}/offers/want`)
- ✅ Запрос карт пользователя (`/trades/{id}/availableCardsLoad`)

## 🐛 Исправление ошибки 422

### Проблема
У владельцев с большим количеством карт (>9999) не находился instance_id карты, хотя карта у них была. Это происходило потому что:
- API `availableCardsLoad` работает с offset
- offset указывает НЕ на номер карты, а на номер **батча**
- Батч = 10000 карт

### Решение
Теперь скрипт:
1. Начинает с offset=0 (карты 1-9999)
2. Если не нашел - переходит к offset=10000 (карты 10000-19999)
3. Продолжает до offset=20000, 30000, и т.д.
4. Останавливается когда API возвращает пустой результат

## 📊 Мониторинг работы

### Во время работы вы увидите:

```
🌐 Proxy: http://***@proxy.com:8080
⏱️  Rate Limiting: 66 req/min
🔐 Вход в аккаунт...
✅ Авторизация успешна

📊 Текущий rate: 5/66 req/min

🔍 Поиск instance_id карты 75578 у владельца 849862...
  Проверка батча offset=0
  Проверка батча offset=10000
✅ Найден instance_id=213230891

⚠️  Rate limit pause: 14.2s
```

### Индикаторы:
- `🌐` - Информация о прокси
- `⏱️` - Rate limiting
- `📊` - Текущая загрузка
- `⏸️` - Пауза из-за rate limit
- `⚠️` - Получен код 429

## 🛠️ Устранение неполадок

### Прокси не работает

1. Проверьте формат URL прокси
2. Убедитесь что прокси поддерживает HTTPS
3. Для SOCKS5 установите: `pip install requests[socks]`
4. Проверьте авторизацию (если требуется)

### Часто появляется 429

1. Уменьшите `RATE_LIMIT_PER_MINUTE` в config.py
2. Увеличьте задержки между запросами
3. Используйте прокси (может помочь)

### Карты не находятся (422)

1. Убедитесь что используется обновленная версия trade.py
2. Проверьте что `CARDS_PER_BATCH = 10000`
3. Включите `--debug` для подробных логов

### Медленная работа

1. Это нормально - rate limiting работает
2. Не отключайте rate limiting - получите бан
3. Используйте прокси для параллельной работы нескольких ботов

## 📝 Лучшие практики

1. **Всегда используйте rate limiting** - не меняйте лимиты на более высокие
2. **Используйте прокси** - защита от бана и возможность запуска нескольких ботов
3. **Включайте мониторинг** - `--enable_monitor` для автоматической обработки
4. **Сначала тестируйте** - используйте `--dry_run` для проверки
5. **Следите за логами** - включите `--debug` при проблемах

## 📄 Лицензия

MIT License

## 🤝 Вклад

Pull requests приветствуются. Для больших изменений сначала откройте issue.

## ⚠️ Дисклеймер

Этот скрипт предназначен только для образовательных целей. Используйте на свой страх и риск. Автор не несет ответственности за блокировку аккаунта или другие последствия использования.